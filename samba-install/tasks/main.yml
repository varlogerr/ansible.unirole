---
- include_vars: "{{ xfacts.distro.osfamily }}.yml"

- block:
  - name: install samba
    apt: name=samba state=present
    when: xfacts.distro.osfamily == 'debian'

  - name: start samba server
    service: name={{ _samba_install.daemon }} state=started enabled=yes
  become: true
